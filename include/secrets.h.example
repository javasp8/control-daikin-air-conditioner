/**
 * secrets.h.example
 *
 * WiFi認証情報などの秘密情報を格納するファイルのテンプレート
 *
 * セットアップ手順:
 * 1. このファイルをコピーして secrets.h という名前で保存
 *    コマンド: cp include/secrets.h.example include/secrets.h
 *
 * 2. secrets.h を開いて、YOUR_WIFI_SSID と YOUR_WIFI_PASSWORD を
 *    実際のWiFi情報に書き換える
 *
 * 3. secrets.h は .gitignore に含まれているため、Gitにコミットされません
 *
 * ⚠️ 注意: このテンプレートファイル(.example)はGitにコミットされますが、
 * 実際の認証情報を含む secrets.h はコミットされません。
 */

#ifndef SECRETS_H
#define SECRETS_H

// WiFi認証情報
// ⚠️ 以下の値を実際のWiFi情報に書き換えてください
namespace WiFiSecrets {
  const char* SSID = "YOUR_WIFI_SSID";        // ← あなたのWiFi SSIDに置き換える
  const char* PASSWORD = "YOUR_WIFI_PASSWORD"; // ← あなたのWiFiパスワードに置き換える
}

// 将来的に追加する可能性のある他の秘密情報
// 例: APIキー、トークンなど
// namespace ApiSecrets {
//   const char* API_KEY = "YOUR_API_KEY";
// }

#endif // SECRETS_H
